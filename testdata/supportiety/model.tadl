#! {
    // This document contains model definitions that are shared across all contexts.
    // There are three model types: Value, Entity, Aggregate.

    ## Address describes a housing address without person specific information.
    Value Address {
        Street      string,
        HouseNumber int,
        City        string,
        ZipCode     int,
    }

    ## Person is a person living somewhere.
    Entity Person {
        Id UUID,
        Firstname string,
        Lastname string,
        Address Address,
    }

    ## Ticket describes a support ticket that has some text and a type (bug report, feedback, etc.)
    Entity Ticket {
        Id UUID,
        Text string,
        Type string,
        Time int,
    }

}