package yaml

import "github.com/golangee/architecture/ast"

// ParseDir parses all .yml/.yaml files from the given path and returns them in a nested hierarchy.
func ParseDir(pset *ast.PositionSet, path string) (*ast.Obj, error) {

}
