glossary:
  book repository:
    name: BookRepository # be explicit (=verbose) or use naming rules?

    # ellipsis documentation string is replaced with the type identifier to decrease verbosity.
    # Lower writing determines a keyword, in this case "doc".
    doc: ...is a repository to handle books.

  moderator:
    name: Moderator
    doc: ...